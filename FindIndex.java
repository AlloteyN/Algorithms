class Solution {
    public int strStr(String haystack, String needle) {
        
    return haystack.indexOf(needle);
        // StringBuffer x = new StringBuffer(haystack);
        // StringBuffer y = new StringBuffer(needle);
        
        
        
    }
}
